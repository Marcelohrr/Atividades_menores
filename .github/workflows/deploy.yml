name: Deploy GitHub Pages

on:
  push:
    branches:
      - Curso_em_Vídeo
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      # 1. Pega a branch Curso_em_Vídeo,
      # que atualmente é a base do seu site
      - name: Checkout site atual
        uses: actions/checkout@v4
        with:
          ref: Curso_em_Vídeo
          path: site

      # 2. Pega a branch freeCodeCamp,
      # onde estão os projetos React
      - name: Checkout projetos React
        uses: actions/checkout@v4
        with:
          ref: freeCodeCamp
          path: react-projects

      # 3. Instala o Node
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: react-projects/React/ChefGastro/package-lock.json

      # 4. Instala as dependências do ChefGastro
      - name: Install dependencies
        working-directory: react-projects/React/ChefGastro
        run: npm ci

      # 5. Gera o build do ChefGastro
      - name: Build ChefGastro
        working-directory: react-projects/React/ChefGastro
        run: npm run build

      # 6. Coloca o ChefGastro compilado dentro
      # da estrutura que será publicada
      - name: Copy ChefGastro
        run: |
          rm -rf site/React/ChefGastro
          mkdir -p site/React/ChefGastro
          cp -r react-projects/React/ChefGastro/dist/. site/React/ChefGastro/

      # 7. Configura o GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # 8. Prepara tudo que está em "site" para publicação
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

      # 9. Publica
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4